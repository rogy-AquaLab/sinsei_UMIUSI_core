{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "colcon: build",
      "type": "shell",
      "command": "ROOT_DIR=$(dirname $(dirname ${workspaceFolder})) && cd $ROOT_DIR && colcon build --symlink-install"
    },
    {
      "label": "ros2: launch",
      "type": "shell",
      "dependsOn": [
        "colcon: build"
      ],
      "command": "ROOT_DIR=$(dirname $(dirname ${workspaceFolder})) && cd $ROOT_DIR && source install/local_setup.bash && ros2 launch sinsei_umiusi_core main.yaml "
    }
  ]
}
